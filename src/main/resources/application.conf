default {
  url = "jdbc:postgresql://localhost/postgres?user=postgres&password=hamer12345" // change me
  url = ${?JDBC_DATABASE_URL} // вот тут для хероку url надо вписать если деплоить бушь
}

ru.tinkoff.fintech.stocks {
  port = 8080
  port = ${?PORT}
  
  db {
    url = ${default.url}
    driver = "org.postgresql.Driver"
  }
}

akka {
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  loglevel = "DEBUG"
  logging-filter = "akka.event.slf4j.Slf4jLoggingFilter"
}