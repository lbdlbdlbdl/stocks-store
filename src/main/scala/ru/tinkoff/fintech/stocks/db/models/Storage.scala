package ru.tinkoff.fintech.stocks.db.models

final case class Storage(
                          login: String,
                          idStocks: Long,
                          count: Int
                        )

